// Tiny shared helpers used across the other JS files.
// This file must be loaded first (before navigation.js, hero.js, etc).
var $ = (s) => document.querySelector(s);
var $$ = (s) => document.querySelectorAll(s);